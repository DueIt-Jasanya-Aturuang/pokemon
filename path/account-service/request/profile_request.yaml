Response200:
  description: OK
  content:
    application/json:
      schema:
        type: object
        properties:
          data:
            $ref: '../../../component/schemas/profile.yaml#profile'
